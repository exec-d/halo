<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
    <!-- Textes des widgets (RemoteViews) : ombre sombre qui les détache du
         fond d'écran, couleurs du téléphone. -->
    <style name="HaloLabel">
        <item name="android:fontFamily">monospace</item>
        <item name="android:includeFontPadding">false</item>
        <item name="android:letterSpacing">0.06</item>
        <item name="android:maxLines">1</item>
        <item name="android:ellipsize">end</item>
        <item name="android:shadowColor">@color/text_shade</item>
        <item name="android:shadowRadius">6</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:textColor">@color/clock_line</item>
        <item name="android:textSize">10sp</item>
    </style>
    <style name="HaloValue">
        <item name="android:fontFamily">sans-serif</item>
        <item name="android:includeFontPadding">false</item>
        <item name="android:maxLines">1</item>
        <item name="android:ellipsize">end</item>
        <item name="android:shadowColor">@color/text_shade</item>
        <item name="android:shadowRadius">8</item>
        <item name="android:textColor">@color/clock_core</item>
        <item name="android:textFontWeight">800</item>
        <item name="android:textSize">18sp</item>
    </style>
    <style name="HaloDetail">
        <item name="android:fontFamily">monospace</item>
        <item name="android:includeFontPadding">false</item>
        <item name="android:maxLines">1</item>
        <item name="android:ellipsize">end</item>
        <item name="android:shadowColor">@color/text_shade</item>
        <item name="android:shadowRadius">5</item>
        <item name="android:textColor">@color/clock_line</item>
        <item name="android:textSize">11sp</item>
    </style>
    <style name="HaloConsole" parent="HaloDetail">
        <item name="android:textColor">@color/clock_core</item>
        <item name="android:textSize">12sp</item>
    </style>
</resources>